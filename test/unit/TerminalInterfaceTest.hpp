#ifndef TERMINAL_INTERFACE_TEST_HPP_INCLUDED
#define TERMINAL_INTERFACE_TEST_HPP_INCLUDED

#include "interface/TerminalInterface.hpp"

#include <gtest/gtest.h>

namespace test::unit {

//--------------------------------------------------------------------------

TEST(TerminalInterfaceTestSuite, request)
{
	// NOTE: test must fail until it gets implemented
	EXPECT_TRUE(false);
}

//--------------------------------------------------------------------------

TEST(TerminalInterfaceTestSuite, reply)
{
	// NOTE: test must fail until it gets implemented
	EXPECT_TRUE(false);
}

//--------------------------------------------------------------------------

} // test::unit

#endif // TERMINAL_INTERFACE_TEST_HPP_INCLUDED
